# Contributing

- Branch from `dev` for new features: feature/<name>
- Small commits with descriptive messages.
- Run `make` and test all user flows before opening a PR.
- Use issues to report bugs or request features.
- Include steps to reproduce and expected behavior in issues.
